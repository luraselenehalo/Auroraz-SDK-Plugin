<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8" />
  <title>__NAME__</title>
  <link rel="stylesheet" href="app.css" />
</head>
<body>
  <main>
    <h2>__NAME__</h2>
    <p id="status">Initializing…</p>
    <p class="muted">This is your plugin's sandboxed UI. Edit
      <code>ui/index.html</code>, <code>ui/app.css</code>, and
      <code>ui/app.js</code> to build it out. Cross-origin postMessage
      to the parent window uses the AURORAZ Plugin postMessage protocol.</p>
    <pre id="output">(waiting for auroraz/init)</pre>
  </main>
  <script src="app.js"></script>
</body>
</html>
