# __NAME__

__DESCRIPTION__

Generated by AURORAZ Plugin Wizard.

## Layout

* `plugin.yaml` — manifest (id, version, permissions, entry points)
* `__init__.py` / `backend/main.py` — plugin code
* `ui/` — sandboxed iframe UI (only present if you opted in)

## Next steps

1. `POST /api/plugins/__PLUGIN_ID__/install` to register this plugin
2. `POST /api/plugins/__PLUGIN_ID__/enable` to activate it
3. Run the linter: `GET /api/plugins/__PLUGIN_ID__/lint`
4. Iterate. Reload via the Plugin Page's "Reload" button.

## Permissions

This plugin declares the following permissions (canonical form):

__PERMISSIONS_YAML__

Add more in `plugin.yaml` and re-run lint to surface mismatches between
declared permissions and code.
